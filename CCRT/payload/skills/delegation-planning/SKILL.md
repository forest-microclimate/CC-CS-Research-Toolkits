---
name: delegation-planning
description: Invoke WHEN planning HOW to distribute a task across subagents — map each subtask to the specialist AGENT (the why/which) + SKILL (the how) that fits it, assign each delegated subtask a difficulty tier -> model (Part D tier table; claude-opus-5 only as a supervised delegate:opus5-executor child under a Planner's active watch, never the session default, never a coordinator or sub-planner, never bare opus), and decide whether to run a multi-agent cascade and which of the four topologies (parallel-wave / sequential-build / convergence / verify-loop) matches the work, or rule cascade OUT for tightly-coupled single-thread work. Owns the decision procedure + machine-readable routing block behind the Delegation & Routing section every /plan now requires. Fires on "which specialist/skill for this subtask", "should this be a cascade", "which topology", "which model for this child", "how do I decompose this across subagents", "route this plan". NOT the planner persona (-> planner agent) and NOT concurrency sizing (-> preflight-parallel).
---
<!-- SPDX-License-Identifier: MIT · Copyright (c) 2026 Neill Prohaska <forest.microclimate@gmail.com> -->
# STATUS: CURRENT (2026-07-19). Authored for Claude Code from the v2.0 delegation-planning skill (reverse port); orchestration semantics mapped to the Task tool (subagents share the working tree; run-to-completion), no auto-load kernel. AMENDED 2026-07-30 (H-a merge): the coordinator>=child ORDERING and the "cannot steer mid-run" claim are BOTH retired and superseded IN-BAND where each operates (Part D's tier table and the verify-loop topology) — not restated here. AMENDED 2026-08-06 (Z1, kit v1.7): Part D's T1_hardest route gains the verified-launch note (warmup + watchdog by ~call 5). AMENDED 2026-08-06 (Z7): Part D's routing-block field list gains `steps` (required when topology ≠ single-thread) + the gate's L7/L8 enforcement statement (owner-completeness / multi-stage completeness).

# delegation-planning — route subtasks to specialists/skills and pick the cascade topology
# WHAT: the decision procedure for a plan's **Delegation & Routing** — (A) which specialist AGENT + SKILL owns each subtask, (B) whether the work earns a cascade, (C) which of four topologies fits, (D) the section a /plan must emit. This is the HOW of routing; it does not carry the planner persona or execute anything.

## When to invoke
WHEN building a plan and about to decide who does each part => apply this. Concretely: writing the Delegation & Routing section a `/plan` now mandates · choosing between doing a step yourself vs launching a subagent (the Task tool) · deciding whether a step is a single-agent job or a multi-agent cascade · picking a cascade topology · matching a subtask to one of the toolkit's specialist agents.

## The signature error this prevents
**Reflexive cascade + charter-mismatch routing** — the two failures this skill exists to stop:
1. Fanning out tightly-COUPLED single-thread work (authoring one coherent doc, a context-heavy edit, a single-point decision) into subagents that each lose the context the work depends on — overhead with no parallelism gain.
2. Routing a subtask to a specialist whose ONE job does not cover it (e.g. a backward-looking rational-reconstruction specialist handed forward-looking orchestration) — a plausible-looking assignment that quietly produces the wrong kind of work.
A liberal-cascade preference makes #1 the standing risk; the fix is not fewer cascades but RIGHT ones — route by coupling and charter, not by reflex.

## Load-bearing move — route by COUPLING + INDEPENDENCE, not by reflex
The default execution mode is **single-thread (do it yourself)**. A delegation or cascade must EARN its place against its cost. Decide in this order:
1. Does the subtask name a distinct discipline/method one of the toolkit's specialist agents owns? => route to that agent + its skill (Part A).
2. Do the pieces have genuinely INDEPENDENT tracks, or does one phase bloat context before the next needs a clean slate, or does the judgment want independent viewpoints, or does the output need adversarial verification? => a cascade earns its place (Part B) — pick the topology by dependency shape (Part C).
3. Otherwise (coupled, single-thread, context-heavy, single-point) => do it yourself; naming a specialist as the notional owner is fine, but do NOT fan it out.
"Value-driven, no friction" is a valid answer: if the honest reason to keep something single-thread is "it's coupled and I hold the context", say that — a fabricated parallelism is worse than none.

## Part A — subtask -> specialist AGENT + SKILL (the routing move)
The customization layer splits every job in two: a **specialist AGENT owns the WHY/WHICH** (method selection + interpretation, disclaims implementation) and a **SKILL owns the HOW** (the hard-won procedure). So routing a subtask = name BOTH: the specialist whose one-job covers the subtask's dominant discipline, AND the skill(s) that specialist loads to do it.
- STEP ZERO — recognize the ARCHETYPE first. WHEN about to route a task/subtask => FIRST name its request archetype via the `request-archetypes` registry (handoff document, planning, skill/agent authoring, code review, data indexing, figure, methods doc, stats-method choice, manuscript, ...); adopt that archetype's canonical specialists + skills + OUTPUT FORM as the default, THEN refine per the coupling rules below. The registry is the empirical shortcut to the right carriers; this Part A is how you adjust when the archetype is ambiguous or spans two. A handoff/consolidation request => output FORM is ALWAYS machine-md (via `baton`), NEVER freehand prose — the single most-missed mapping.
- WHEN a subtask names a distinct discipline/method => name the specialist + skill for it (e.g. "recover the design rationale" -> `design-rationale-analyst` agent + `design-rationale` skill; "tighten this prompt" -> `prompt-engineer` agent + `eliciting-llm-behavior` skill; "author a hook/skill/agent" -> `agent-tooling-engineer` + `toolkit-extension-authoring`/`machine-md`; "implement/refactor a pipeline, module, or CLI" -> `software-developer` + `software-craft` [+ `testing-discipline`/`refactoring` as the work demands], with `code-review-debugger` as the verify-loop reviewer).
- WHEN no specialist's charter fits => say so — it is main-agent / generalist work. Do NOT force-fit to the nearest-sounding specialist; charter-mismatch is the silent failure (see signature error #2).
- WHEN the subtask is purely a library/procedure with no persona judgment => a SKILL alone may suffice (load it, no delegation).
- WHEN a subtask is SINGLE-THREAD but a specialist's discipline applies => name that specialist as the NOTIONAL owner and mark it "single-thread — channel that discipline / load its skill myself, no delegation"; naming a specialist as OWNER does NOT imply fanning out to it.
- WHEN the main agent executes a phase but a specialist should advise ONE sub-decision (a soft/partial charter fit) => OWNER = "main agent, executing; <specialist> advises on <the sub-decision>" — owner need not be a single pick.
- WHEN the task is to AUTHOR-from-scratch but the fitting specialist's charter is REVISE (e.g. a prose stylist) and the content-context lives with the main agent => do NOT delegate authoring to a reviser that cannot know the context; keep it main-agent single-thread and load the specialist's skill for the craft. Delegating context-blind is charter-coupling mis-routing (signature error #2).
- To SEE the roster before routing: read the agent files in `.claude/agents/` (each names its one job in its description) and the skills catalog (`.claude/skills/`, or `search_skills`). Match to the description's stated job, not the name.

## Part B — should this be a cascade? (the WHETHER)
A cascade (any multi-agent structure) earns its place when AT LEAST ONE holds; otherwise stay single-thread:
- **Independence** — the work splits into tracks with no cross-dependency that can run at the same time (-> parallel-wave).
- **Context isolation** — one phase would bloat the working context before a later phase needs a clean slate (-> sequential-build across a handoff).
- **Independent viewpoints** — a judgment is stronger from several divergent lenses than one (-> convergence).
- **Adversarial check** — the output is high-consequence and checkable, so it should be attacked and repaired before shipping (-> verify-loop).
Grade the tradeoff explicitly:
- COST of delegating: a subagent sees ONLY its task brief — never your conversation — so coupled/context-heavy work loses more to that blindness than it gains; plus coordination overhead. And a Task-tool subagent runs to completion and returns ONE report — [SUPERSEDED 2026-07-30, MEASURED 2026-07-29] ~~you cannot steer it mid-run~~ a correction/addendum CAN be queued to a running child via SendMessage (delivered at its next tool round), but a badly-SCOPED brief needs a REDESIGN — a wasted round-trip, not a live fix.
- BENEFIT: real parallelism (wall-clock), context isolation (a clean slate per track), independent adjudication (bias reduction), or adversarial hardening.
- Cascade WHEN benefit > cost. WHEN the only "benefit" is that fanning out feels thorough => that is reflex, not benefit — stay single-thread.
- SYMMETRIC guard (the under-cascade error): WHEN work IS genuinely independent (N units, no cross-dependency), OR a judgment genuinely needs independent viewpoints, OR a high-consequence output is checkable => do NOT force single-thread out of caution — keeping earning work serial mis-routes as surely as reflexive fan-out. The single-thread default breaks TIES for COUPLED work; it is not a thumb on the scale against real independence.

## Part C — the four topologies (the WHICH)  [reference]
Pick by the dependency shape between units. Fan-out runs as several Task-tool subagent calls launched together in one message (concurrent); issued one at a time they run serially. All four topologies are MULTI-AGENT (they spend delegation cost); a task you run yourself step-by-step in ONE context is single-thread — even when its steps are ordered or data-dependent — do NOT label that a cascade.

1. **PARALLEL-WAVE (fan-out)** — N units, DIFFERENT input each, SAME operation, NO inter-dependence.
   WHEN: homogeneous independent work — screen each compound family, adjudicate each transcript segment, review each scenario.
   SHAPE: launch one Task-tool subagent per unit in a single message; each returns its report; collect them; batch-analyze. Size concurrency with `preflight-parallel`.

2. **SEQUENTIAL-BUILD (pipeline)** — track B CONSUMES track A's output AND the tracks should run in SEPARATE agent contexts. The bare A->B data dependency is necessary but NOT sufficient: a data-dependent pipeline ONE agent can hold in a single context is single-thread cross-phase ordering, NOT this cascade. What EARNS sequential-build is CONTEXT-ISOLATION need (Part B) on top of the data dependency — A's context would bloat or distract B.
   WHEN: recover rationale -> then render the explainer in a clean slate; a heavy fetch/build-A phase whose full context B does not need. NOT: two steps that merely happen in order within one context.
   SHAPE: launch A; when A returns, launch a SECOND subagent for B whose brief names A's output FILES BY PATH. Subagents share the working tree in Claude Code, so hand a file down by writing it to a tracked path — do NOT expect B to see A's in-memory state.

3. **CONVERGENCE (multi-voice adjudication)** — M agents attack the SAME input from DIFFERENT lenses, then a lead synthesizes.
   WHEN: a judgment benefits from independent viewpoints / single-reviewer bias is a risk — e.g. a multi-voice enforcement pass over a requirements set, blind readers over one document.
   SHAPE: parallel fan-out with DIVERGENT briefs (a different lens per voice — enforcer / minimalist / threat-modeler / maintainer...) -> lead converges the verdicts. DISTINCT from parallel-wave: same input + different lenses + a merge step, vs different inputs + same operation. A convergence with identical briefs is just redundant parallel-wave — give each voice its own lens or drop it.

4. **VERIFY-LOOP (generate -> adversarial check -> patch)** — produce, hand to an ADVERSARIAL reviewer told to find fault, patch, repeat until clean.
   WHEN: output correctness is high-consequence AND checkable — a shipped artifact, a rule that must fire reliably, a composed figure.
   SHAPE: build -> launch a review subagent with a fault-finding brief (ask for a PARSEABLE verdict — a fixed schema in the brief) -> converge -> re-run only the affected pieces -> loop, BOUNDED (<=3 rounds). The reviewer is a FRESH instance re-tasked as adversary — a fresh subagent, or a domain specialist (e.g. `code-review-debugger`) re-briefed to attack. Because re-tasking a subagent as an adversary is a REDESIGN, not a live correction (a queued SendMessage delivers only at the child's next tool round — steering fits corrections/addenda/stop, not a new assignment), each round is a fresh launch with the prior round's output named by path. A friendly re-read is not a verify-loop; the reviewer must be adversarial and fresh.
   TERMINAL BRANCH: WHEN output still fails at the round bound => FAIL CLOSED — escalate / surface for user decision / withhold; NEVER ship the best-effort artifact. For a SAFETY-CRITICAL output (an enforcement rule, a gate) shipping unverified is the exact failure the loop guards against.
   NOT a verify-loop: an INNER diagnostic iteration inside one coupled task — fit -> check convergence -> refit, edit -> re-run tests — is SINGLE-THREAD self-correction. "High-consequence AND checkable" names the trigger, but a quality check you perform yourself does not earn a separate adversarial agent.

Topologies COMPOSE: a build phase (single-thread OR a sequential-build) can be FOLLOWED BY a verify-loop — the build happens-before the verify; this does NOT require the build itself to be a two-agent sequential-build. A parallel-wave's units can each be a small pipeline.

## Part D — the Delegation & Routing section a /plan must emit (the WHAT)
Every plan produced under `/plan` carries an explicit Delegation & Routing section. Minimum content, per phase/track:
- **Archetype** — the recognized request archetype (from `request-archetypes`) and thus the canonical OUTPUT FORM the phase must produce (e.g. handoff => machine-md). WHEN no archetype matches => say "novel — generic handling" rather than force-fitting.
- **Owner** — the specialist AGENT + SKILL(s) that do it (Part A), OR "main agent, no specialist fits", OR "skill-only, no delegation".
- **Execution** — a MACHINE-DETECTABLE tag, exactly one per node: `EXECUTION: main-agent` (you run it inline) OR `EXECUTION: CODE:<cmd>` (a script/CLI computes it — the instrument test's first-class outcome) OR `EXECUTION: delegate→<agent>` (a Task-tool launch) OR a named cascade TOPOLOGY (Part C), each with its one-line reason. `delegate→<agent>` is a COMMITMENT: that child must actually be launched — declaring delegate and then running the node yourself is the laundering failure (the recorded case: six declared delegation tracks, four self-run). After each wave, diff declared routing against the Task launches actually made.
- **Model tier + effort (delegated nodes)** — every `delegate→` node names its tier + effort. THE TIER TABLE BELOW IS THE SINGLE SOURCE on Claude Code ([SUPERSEDED 2026-08-04 — the unpin] ~~agents' frontmatter is pinned to full IDs and `lib/verify_models.sh` gates the payload~~ — agents carry NO frontmatter model pin, so THE LAUNCH decides the model, and `lib/verify_models.sh` fails the payload on any `model:` key in `payload/agents`). The **model** column is the tier's ID OF RECORD; the **launch** column is how you get it, under the four-rank precedence MEASURED 2026-08-04, highest wins: (1) the `CLAUDE_CODE_SUBAGENT_MODEL` env var overrides both of the next two — keep it at `inherit` · (2) the Task `model` param, ALIASES ONLY `{sonnet|opus|haiku|fable}`, full ids rejected with a live InputValidationError (measured governing: haiku ×4, sonnet ×5) · (3) agent frontmatter `model:`, where full ids ARE legal (measured governing: a full-id pin resolved `claude-opus-5` ×3) · (4) inherit the main model. [SUPERSEDED 2026-08-04 — an omitted param requests the MAIN model, not fable] ~~OMIT ⇒ fable~~: omission is RANK 4 — it requests whatever the session itself runs on, so NAME every tier you want (stated once here; the cells below point back):
  | tier | model | launch | effort | scope |
  |---|---|---|---|---|
  | T1 | `claude-opus-4-8` | project SHADOW-PIN (`lib/crt-dev-model.sh`) — a rank-3 frontmatter pin scoped to one project, since the launch takes no full ids; [SUPERSEDED 2026-08-04] ~~OMITTING instead gets fable, an ESCALATION, which is the safe direction~~ (omission is rank 4 — see the intro above) | max | hard/long/complex reasoning or complex code (a large coupled research-model codebase — e.g. a Julia land-surface model — deep stats design, multi-step derivations) |
  | T1_hardest | `claude-fable-5` | `delegate:fable-executor` — PARAMLESS, its own rank-3 pin governs: THE BUILT ROUTE (spec in the serving note below). LAUNCH VERIFIED (2026-08-06) — warmup reads open the brief's task (persona/skill Read-pointers ARE the warmup) + the shipped watchdog certifies serving by ~call 5 (the `model-verification` skill's `fable_watchdog.py`); relaunch on SWAPPED. Else name `fable` on the param (rank 2) or pin `claude-fable-5` in frontmatter (rank 3) WHEN the child needs tools that executor lacks — a SUB-PLANNER above all, which holds no Agent grant there and so could not launch its own children; [SUPERSEDED 2026-08-04] ~~OMIT the model param~~ (rank 4 — see the intro above) | max | the hardest / most novel subtasks (escalation of T1, planner judgement). ROUTING DEFAULT: work that FITS the `Read, Edit, Write, Grep, Glob, Bash` grant — high-order reading, writing, review, code — goes to `fable-executor` FIRST; `opus5-executor` (T1_supervised) takes what genuinely needs the FULL toolset (web fetches, Skill-tool mechanics) or its long-horizon supervised charter. A specialist PERSONA rides such a child as a Read-pointer to the persona file, the executor's contract outranking it on conflict; SKILLS reach it the same way, by `SKILL.md` path. SERVING CAVEAT: a direct fable request from a FULL-TOOLS child is still SUBSTITUTED (note below) |
  | T2 | `claude-sonnet-5` | `sonnet` | high | medium synthesis / review / standard analysis code |
  | T3 | `claude-sonnet-5` | `sonnet` | medium | SIMPLE rough-draft code ONLY (never research-model-class code); promote to a T1 review before trusting |
  | T4 | `claude-haiku-4-5` | `haiku` | low | high-volume mechanical fan-out (sweeps, formatting, extraction) |
  | T1_supervised | no tier model — the project-scoped `opus5-executor` agent's OWN frontmatter carries it (`claude-opus-5`) | `delegate:opus5-executor` + `"supervised": true` — name NO model field | high | the CONSTRAINED SUPERVISED class (2026-08-04): a tightly-scoped CHILD under a Planner's ACTIVE watch for scope-drift, thrash, and false-positive over-caution. REACH FOR IT on a long-horizon task whose spec is COMPLETE (scope written down, done-condition checkable, sustained execution rather than a fresh judgement each step) — never by reflex. The token names the SUPERVISION ARRANGEMENT, not a model, so the track MUST carry the marker (see SUPERVISED-EXECUTOR TRACKS below) |
  [SUPERSEDED 2026-08-04 — constrained supervised use; see the `T1_supervised` row above + SUPERVISED-EXECUTOR TRACKS below] ~~BANNED: `claude-opus-5` — any tier, any call (user bar: much higher observed failure-mode rate)~~ — the live rule is the `T1_supervised` row above: PERMITTED in exactly ONE shape, a supervised child under a Planner's active watch. STILL BANNED, no exceptions: as the session default · as a coordinator or a sub-planner (it is a worker, never a router) · the bare `opus`/`opusplan` aliases (CC >=2.1.219 resolves them to Opus 5, and an alias re-resolves silently whenever CC remaps it). The two retired INVOCATION atoms stay greppable so a search lands on the correction (the live rule is the four ranks in the intro): [SUPERSEDED — omission is rank 4 and requests the MAIN model; the old rule rested on a child self-report the serving stamps falsified] ~~a T1_hardest launch NAMES NO MODEL AT ALL — omitting the model parameter is the correct and only safe way to get fable~~ · [SUPERSEDED — REAL observation, wrong cause: serving-side SUBSTITUTION of fable requests, not alias resolution] ~~naming fable explicitly can SILENTLY resolve to `claude-opus-5`~~. SERVING SUBSTITUTION (an OPEN vendor bug, filed 2026-08-04): a correct fable REQUEST is not yet a fable RUN. One measured session population (134 children): fable-resolved launches from FULL-TOOLS subagents were served `claude-opus-5` ~94% of the time (49/52), RESTRICTED-TOOLS agents were largely faithful (6/7 historical, 3/3 controlled), every other id faithful. A SECOND population on a different main model ran 9/9 substituted (0 fable stamps in 733 calls) — a different main model is NO escape. Discriminator ISOLATED single-variable to the **Skill tool grant**: `Read, Grep, Skill` served opus-5 3/3 where `Read, Grep` held fable 2/2, with `+Agent`, `+Bash`, and the web grants each faithful 2/2 and request size exonerated to ~87k. Measured SUFFICIENT, not necessary — a ≳90k size-like residual is unprobed, one restricted-tools run swapped MID-RUN with every visible request field identical, and the mechanism is vendor-side and unknown, so a restricted schema is a better bet, not a guarantee. THE BUILT ROUTE follows: `delegate:fable-executor` — the T1_hardest grant above, holding NEITHER Skill (the trigger) NOR Agent, pinned `claude-fable-5`, launched paramless — passed a 5/5 serving-stamp acceptance, `fixture-measured`, the first reliable fable route since the substitution epoch. VERIFICATION LAW: a model claim is verified by the SERVING STAMP (the API response's own `model` field on each assistant turn of the child transcript) or it is not verified — child SELF-REPORT is DISQUALIFIED (wrong 3 of 5, measured), and the UI header shows the launch-time RESOLVED model, which is intent. Grep the stamp at collect whenever a routing claim depends on it (the `model-verification` skill runs it). [SUPERSEDED 2026-07-30 — ordering RETIRED] ~~ORDERING: the planner runs at >= any child it routes.~~ CAPABILITY: `T1_hardest` is ALWAYS available to a child regardless of the planner's own tier — planner authority is positional and receipts-adjudicated (detail in the planner agent's Model routing). Brief strictness is tier-conditional: T4 briefs carry the hard checkpoint block; T2/T3 standard; T1 light-touch.
- **Brief + access defaults (delegated nodes)** — `record_access: self-service` (the brief names the files/paths the child reads itself, never only a précis; deviations state why); a `brief_ref` for long work (the brief persisted to a file so a crashed session can re-launch from the record); report length capped; the stuck-escalation rule included verbatim.
- **Recon** — each commit-class node STATES its `recon_done` evidence (the ls/count/read that surveyed the territory) or `stated-none:<why>`. Stated, not adjudicated.
- **Reserved** — a node the user reserved for collaborative mode carries `reserved_for_user: true`; under /solo such a node is read-only until the user weighs in.
- **Tradeoff** — where a cascade is chosen, the benefit that beats the delegation cost; where single-thread is chosen for work that looked parallelizable, why.
- **Concurrency (fan-out only)** — where the execution is a parallel-wave, note the unit count and that concurrency is sized against real headroom (`preflight-parallel`); a "fan out N units" line with no sizing is incomplete.
- **Level** — a topology can hold BETWEEN phases (a sequential-build ordering the phases) while each phase is single-thread WITHIN itself; state which level a cascade label applies to. Do NOT read a cross-phase ordering as a mandate to delegate each phase.
- **Unresolved fork** — WHEN a subtask's correct owner depends on a user decision not yet made (two valid specialist routes) => OWNER = "unresolved — <the fork>, pending user choice"; surface it rather than guessing.
**The machine-readable form (the A2 routing block).** Emit the section BOTH as the prose bullets above AND as a routing table or ONE fenced `routing` JSON block (fields: id, task, archetype, owner, executor, topology, steps — required when topology ≠ single-thread: a JSON array of ≥2 stage descriptions, one per stage of the cascade (a markdown-table cell carries the array literally) — model_tier, effort, detached, brief_ref, record_access, recon_done, reserved_for_user, scope_ref, tradeoff, concurrency — plus `supervised`/`supervision` on a supervised-executor track, below) — the plan is thereby an artifact later gates diff against. ENFORCEMENT on Claude Code: `hooks/plan-routing-gate.sh` (PreToolUse on ExitPlanMode) BLOCKS leaving plan mode when the block/table is missing or malformed — missing executor tags, a `delegate:` naming self, or a banned model/alias in a model-type field. Fix the plan, not the gate.
- L7/L8 (Z7, 2026-08-06): the gate also DENIES the silent routing omissions — a `delegate:` track whose `owner` merely restates the executor tag (record the persona/skill assignment, or the explicit "no specialist fits" / "skill-only" fallback); a multi-stage track (topology ≠ single-thread) with no `steps` array; and a plan routing >1 delegate track with no Waves/ordering section. These are MINIMUM-EXPLICITNESS checks: they force the routing decision to be RECORDED, not to be wise.
- SUPERVISED-EXECUTOR TRACKS (A2 v1.1, 2026-08-04). A track whose executor is `delegate:opus5-executor`, or whose `model_tier` is `T1_supervised`, MUST also carry `"supervised": true` (or `"supervision": "planner-watch"`) — the gate DENIES the track without it, because the permission to run that executor rests entirely on a Planner actively watching the run. Two shape rules follow: name NO model field on such a track (the model comes from that project-scoped agent's own frontmatter, and a raw `model`/`model_id`/`model_tier` value naming it is still a hard FAIL), and DO feel free to name the model in prose fields — owner, task, tradeoff — since the block-wide backstop now fires only on model-FIELD assignments and unparseable blocks.
A plan whose phases each name owner + execution + (where relevant) tradeoff + (delegated) model tier has satisfied the routing mandate. Prose that gestures at "we may delegate" without naming who/which/what-topology has NOT. And do NOT manufacture sub-phases to populate the section: a single COUPLED task is ONE phase with one owner + single-thread — a complete, compliant section, not an under-filled one.

## Non-goals (redirect to the named sibling)
- The planner PERSONA and the always-fire-on-/plan mandate => the `plan` skill (owns the rule) + the `planner` agent (owns the persona). This skill is the HOW they invoke, not the trigger.
- Sizing a fan-out's concurrency against real headroom => `preflight-parallel`.
- Writing the subagent task brief's PROSE so it reliably complies => `eliciting-llm-behavior` (technique) + `machine-md` (form).
- The Task-tool mechanics and agent/skill authoring => `toolkit-extension-authoring`.
- Executing the plan / doing the domain work => the routed specialists themselves.
- Directing a plan once it is RUNNING (harvest -> decide continue/re-route/fix-first/abort/goal-met/adapt) => `directing-execution`.

INVARIANT: route by coupling and independence, not by reflex; name a specialist + skill for each subtask or call it main-agent work; pick the topology that matches the dependency shape; and make every cascade earn its place against its context-loss and latency cost.
REF: `plan` (mandates the Delegation & Routing section this skill fills) · `planner` agent (the persona that drives it) · `directing-execution` (run-time supervise loop) · `preflight-parallel` (concurrency sizing) · `eliciting-llm-behavior` + `machine-md` (the task-brief's technique + form) · `toolkit-extension-authoring` (Task-tool + agent/skill authoring).
